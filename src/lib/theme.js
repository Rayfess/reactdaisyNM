// Central theme names so components don't hardcode strings
export const THEMES = {
  LIGHT: "light",
  DIM: "dim", // matches `src/index.css` DaisyUI config
};

export const DEFAULT_THEME = THEMES.LIGHT;
